#!/usr/bin/env bash

cd "$(dirname "$0")/prepare-commit-msg.d"

for hook in *; do
    bash $hook
    RESULT=$?
    if [ $RESULT != 0 ]; then
        echo "prepare-commit-msg.d/$hook returned non-zero: $RESULT, abort commit"
        exit $RESULT
    fi
done

exit 0
